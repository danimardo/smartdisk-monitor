// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { ActividadDisco } from "./ActividadDisco";
import type { DeviceType } from "./DeviceType";
import type { HealthState } from "./HealthState";
import type { Provenance } from "./Provenance";
import type { UnknownReason } from "./UnknownReason";
import type { VolumeSummary } from "./VolumeSummary";

/**
 * Espejo de `DiskSummary` en `src/lib/design/types.ts`. `enrich_with_smart_data` lo completa con
 * las últimas muestras SMART persistidas; sin ellas (o sin `smartctl_path`) `state` es `unknown`
 * con un motivo explícito, nunca un relleno inventado (FR-004, FR-006).
 */
export type DiskSummary = { id: string, alias: string | null, model: string, deviceType: DeviceType, state: HealthState, temperatureC: number | null, percentageUsed: number | null, 
/**
 * Actividad de disco: media y pico de una ventana deslizante alimentada por muestreo continuo
 * de los contadores de rendimiento (spec `007-actividad-disco-representativa`, ADR-050).
 * Sustituye al antiguo `activity_percent: Option<f64>` (FR-012a). El valor lo pone la ventana
 * en memoria `AppState.actividad`, no una muestra persistida: `enrich_with_smart_data` deja
 * aquí `no_disponible` y los comandos que tienen `AppState` lo sobrescriben con el agregado
 * vivo.
 */
activity: ActividadDisco, powerOnHours: number | null, vendorTempLimitC: number | null, vendorTempCriticalC: number | null, 
/**
 * Autoevaluación SMART global (`smart_status.passed`): `Some(true)` superada, `Some(false)`
 * fallida, `None` sin dato o disco sin SMART. La consume el primer hecho del `HeroPanel`
 * («Salud del firmware», ADR-041).
 */
smartHealthPassed: boolean | null, unknownReason: UnknownReason | null, lastReadAt: string | null, provenance?: Provenance | null, volumes: Array<VolumeSummary>, };
