node_modules
build
.svelte-kit
src-tauri/target
src-tauri/gen
coverage
pnpm-lock.yaml

# Consolidado: se genera, no se edita
historias.md

# Bocetos: marcado generado por la herramienta de diseño, no se reformatea
design

# Recursos de terceros
third-party

# Sistema de diseño: `tokens.css` alinea sus comentarios en columna a propósito, para poder leer
# la escala tipográfica y los materiales de un vistazo. Prettier rompe esa alineación, y es la
# fuente de verdad visual: reformatearla no aporta nada y arriesga a cambiar un valor sin querer.
# Hasta ADR-029 quedaba ignorada sin querer, por coincidir con el patrón `Design-system`.
src/design-system

# Documentación: el formato de las tablas es deliberado y prettier las reflow
docs
*.md

# DTO generados por ts-rs (T029, open-questions.md G.3): `cargo test` los regenera sin pasar por
# Prettier, así que reformatearlos aquí solo crearía una diferencia falsa en la siguiente
# regeneración. `eslint.config.js` los ignora por el mismo motivo.
src/lib/api/generated
